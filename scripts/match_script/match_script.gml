if (matchme = 0 || matchme = 1) && argument0 <= 2
{
    matchme = 2
	if (other.n2 > 3) other.center_gem = id
} 
else if matchme < argument0 
{
    matchme = argument0
	if (other.n2 > 3) other.center_gem = id
}
// anything larger than L's or T's; do not affect those.
