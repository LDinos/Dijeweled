/*if !Gamerule_1.isReplay
{
	//with(iceAppear) event_user(0)
	//with(lockAppear) event_user(0)
	//with(doomAppear) event_user(0)
	//with(skullAppear) event_user(0)
	//with(obj_lock_control) event_user(0)
	//with(obj_doom_control) event_user(0)
	//with(obj_skull_control) event_user(0)
	//with(obj_ice_lock_control) event_user(0)
	//with(obj_bombcontrol) event_user(0) //decrease spawn counter
	//with(obj_coal_control) if !TurnsToSpawnRandom event_user(0) //decrease spawn counter. If its randomized, use event(0) on the spawner instead
}*/