/* PURPOSE */
//An option in Custom Quest editor for ice locks. It tells how many turns it takes for a lock to spawn
on_focus = false //in order to type, we need to be on focus
max_val = 100 //maximum value
min_val = 1 //minimum value
margin = 4 //a little small value after which a text starts
value = "5"
flick = ""