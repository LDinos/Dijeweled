/* PURPOSE */
//This is the AFTER LOCKING object, a lock animation JUST before the gem is fully locked, because a swap was done.
depth = -10
mygem = instance_position(x,y,Gem_1)
audio_play_sound(snd_lock_start,0,0)