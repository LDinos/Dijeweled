/// @description Insert description here
var i_d = async_load[?"id"];

if i_d == dialog
    {
    if async_load[?"status"]
        {
        text = string(async_load[?"result"]);
		send_message()
        }
    }