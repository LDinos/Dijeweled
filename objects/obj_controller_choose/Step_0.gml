/// @description Insert description here
if (mouse_x > x && mouse_x < x+width) && (mouse_y > y && mouse_y < y+height)
	{
		hover = true
		if mouse_check_button_pressed(mb_left)
		{
			var max_index = maxindex
			if global.bot1 max_index = 3
			if (index < max_index) index++
			else index = 0;	
		}
	}
else hover = false
